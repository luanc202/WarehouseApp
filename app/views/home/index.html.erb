Galpões & Estoque

<h2>Galpões</h2>
<% @warehouses.each do |w| %>
  <%= w.name %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  <%= w.area %> m2
<% end %>

<% if @warehouses.empty? %>
  <h2>Não existem galpões cadastrados</h2>
<% end %>