Galpões & Estoque

<h2>Galpões</h2>
<% @warehouses.each do |w| %>
  <%= link_to(w.name, warehouse_path(w.id)) %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  <%= w.area %> m2
<% end %>

<% if @warehouses.empty? %>
  <h2>Não existem galpões cadastrados</h2>
<% end %>